# Ghostty config

This is my default ghostty config. It only adjusts a few default values and the colorscheme.

On Omarchy, Ghostty loads the palette generated from the currently selected Omarchy theme. Catppuccin Mocha remains the fallback on other machines.
