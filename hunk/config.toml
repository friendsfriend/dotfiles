theme = "catppuccin-mocha"   # graphite, midnight, paper, ember, catppuccin-latte, catppuccin-mocha, custom
mode = "auto"        # auto, split, stack
vcs = "git"          # git, jj
watch = true
exclude_untracked = false
line_numbers = true
wrap_lines = false
agent_notes = true
