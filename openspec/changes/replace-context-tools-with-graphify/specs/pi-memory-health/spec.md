## REMOVED Requirements

### Requirement: Memory storage recovery
**Reason**: Memory health diagnostics are removed because the Pi memory store is no longer active.
**Migration**: Use graphify reports/manifest/cost outputs for graph health and remove or manually inspect old memory files if needed.

### Requirement: Memory health diagnostics
**Reason**: Memory health diagnostics are removed because the Pi memory store is no longer active.
**Migration**: Use graphify reports/manifest/cost outputs for graph health and remove or manually inspect old memory files if needed.

### Requirement: Memory entry quality classification
**Reason**: Memory health diagnostics are removed because the Pi memory store is no longer active.
**Migration**: Use graphify reports/manifest/cost outputs for graph health and remove or manually inspect old memory files if needed.

### Requirement: Memory deduplication
**Reason**: Memory health diagnostics are removed because the Pi memory store is no longer active.
**Migration**: Use graphify reports/manifest/cost outputs for graph health and remove or manually inspect old memory files if needed.

### Requirement: Memory lifecycle controls
**Reason**: Memory health diagnostics are removed because the Pi memory store is no longer active.
**Migration**: Use graphify reports/manifest/cost outputs for graph health and remove or manually inspect old memory files if needed.
